trigger AddFedID on User (after insert) {
 
List<User> AllUsers = new List<User>();
    
    for (User GetUser : Trigger.new) {
      User u   = new User();
      EnterFedID.myFedID(GetUser.ID);
      u.id = GetUser.Id; 
      AllUsers.add(u);
    }

}